** Please refer to "Code Modification Checklist" in order to submit any changes to the code **



The key additions to the project's current goals are:
- Raising the project's TRL level to 6 - So no more workarounds and random bugs, solve the problem for once and never look back.
- Implement APIs to enable data to be pulled from the database by a third party - So all data needs to be properly formatted and stored in the database in the appropriate format.
- Making the system flexible to that it can be deployed in any layout - So no more hardcoding stuff.
